---
title: "Draft: Notes on Something I'm Still Writing"
description: "An unpublished draft. It should appear in dev but never in a production build."
pubDate: 2026-08-25
tags:
  - meta
draft: true
---

This post has `draft: true` in its frontmatter.

It exists to demonstrate the draft workflow: it is visible while running
`npm run dev` so you can preview it, but the blog query helper excludes it from
production builds (`npm run build`). You should **not** see this post in the
built `/blog` listing, its own page, the tag pages, or the RSS feed.
